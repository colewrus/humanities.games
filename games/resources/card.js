function Card(introText, outText, _resource){ 
    //card text
        //resources affected
            //amount affected
        //OR
        //instead of tracking resources just put a point toward one outcome
    //Choice results text

    //change the images/art that is visible? 
        //need image attached

    this.introText = introText;
    this.outText = outText; 
    this.r = _resource;
    
}